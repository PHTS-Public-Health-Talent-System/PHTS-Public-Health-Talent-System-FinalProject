import { HeadScopeScopeMembersPage } from '@/features/head-approval/screens/head-scope-scope-members-page';

export const dynamic = 'force-dynamic';

export default function HeadWardScopesPage() {
  return <HeadScopeScopeMembersPage roleTitle="หัวหน้าตึก/หัวหน้างาน" />;
}

