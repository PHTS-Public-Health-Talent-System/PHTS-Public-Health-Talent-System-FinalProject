'use client';

import { HeadScopeSidebar } from './head-scope-sidebar';

export function HeadDeptSidebar() {
  return (
    <HeadScopeSidebar
      role="head-dept"
      roleLabel="หัวหน้ากลุ่มงาน"
      roleBgColor="bg-indigo-600"
      defaultTitle="หัวหน้ากลุ่มงาน"
    />
  );
}
